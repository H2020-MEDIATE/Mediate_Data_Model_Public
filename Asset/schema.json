{
  "$schema": "http://json-schema.org/schema#",
  "title": "Asset",
  "description": "Schema for the MEDIATE Asset entity. An Asset represents a protected or monitored system component registered in the Service Manager registry. It includes asset type, operational status, risk level, associated Sentinel, last assessment timestamp, and registration timestamp.",
  "type": "object",
  "id": {
    "$ref": "https://smart-data-models.github.io/data-models/common-schema.json#/definitions/EntityIdentifierType"
  },
  "required": [
    "id",
    "type"
  ],
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "asset"
      ],
      "description": "NGSI-LD entity type. It has to be asset."
    },
    "assetType": {
      "type": "string",
      "description": "Property. Type or category of the protected asset."
    },
    "operationalStatus": {
      "type": "string",
      "description": "Property. Current operational status of the asset."
    },
    "riskLevel": {
      "type": "string",
      "description": "Property. Current risk level associated with the asset."
    },
    "associatedSentinel": {
      "$ref": "https://smart-data-models.github.io/data-models/common-schema.json#/definitions/EntityIdentifierType",
      "description": "Relationship. Sentinel entity associated with this asset."
    },
    "lastAssessmentTimestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Date and time of the latest assessment associated with this asset."
    },
    "registrationTimestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Date and time at which the asset was registered in the Service Manager registry."
    }, 
    "assetComponents": {
      "type": "array",
      "description": "Property. Static snapshot of software, firmware, hardware, or system components associated with the asset. This information can support CTI integration and asset-level querying.",
      "items": {
        "type": "object",
        "required": [],
        "properties": {
          "componentName": {
            "type": "string",
            "description": "Name of the asset component."
          },
          "identifierType": {
            "type": "string",
            "enum": [
              "cpe",
              "purl"
            ],
            "description": "Type of identifier used for the asset component. Examples include cpe and purl."
          },
          "identifier": {
            "type": "string",
            "description": "Component identifier value, for example a CPE URI or a package URL."
          }
        }
      }
    }
  }
}