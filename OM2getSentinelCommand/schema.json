{
  "$schema": "http://json-schema.org/schema#",
  "title": "GetSentinelCommand",
  "description": "Schema for Service Manager registry retrieval command OM2. This command is sent by the Orchestrator to the Service Manager to request the full representation of a specific Sentinel and its associated Assets.",
  "type": "object",
  "id": {
    "$ref": "https://smart-data-models.github.io/data-models/common-schema.json#/definitions/EntityIdentifierType"
  },
  "required": [
    "id",
    "type",
    "timestamp"
  ],
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "getSentinelCommand"
      ],
      "description": "NGSI-LD entity type. It has to be getSentinelCommand."
    },
    "originator": {
      "type": "string",
      "enum": [
        "ORCHESTRATOR"
      ],
      "description": "Property. Component that generated the command."
    },
    "messageId": {
      "type": "string",
      "description": "Property. Unique identifier of this command message."
    },
    "correlationId": {
      "type": "string",
      "description": "Property. Identifier used to correlate this command with the corresponding SentinelResponseEvent."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Date and time at which the command was generated."
    },
    "sentinelId": {
      "$ref": "https://smart-data-models.github.io/data-models/common-schema.json#/definitions/EntityIdentifierType",
      "description": "Relationship. Sentinel entity requested by the Orchestrator."
    }
  }
}